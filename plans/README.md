# Implementation Plans

Generated by the improve skill on 2026-06-10 against commit `32ac5e5`. Execute in
the order below unless dependencies say otherwise. Each executor: read the plan
fully before starting, honor its STOP conditions, and update your row when done.

Verified baseline at `32ac5e5`: `npm run lint` ✓ · `npm run typecheck` ✓ · `npm test` ✓ (5 files, 30 tests, ~0.6s).

## Execution order & status

| Plan | Title                                                          | Priority | Effort | Depends on                | Status |
| ---- | -------------------------------------------------------------- | -------- | ------ | ------------------------- | ------ |
| 001  | Patch Next.js to 14.2.35 (critical middleware auth-bypass CVE) | P1       | S      | —                         | DONE   |
| 002  | GitHub Actions CI gate + `npm run verify` script               | P1       | S      | —                         | DONE   |
| 003  | Checked DB writes in the AI pipeline + saveTags race fix       | P1       | M      | — (002 recommended first) | TODO   |
| 004  | Dedicated `WORKER_SECRET` for the worker route                 | P1       | S      | —                         | TODO   |
| 005  | Bookmarklet token expiry + schema tightening                   | P2       | S      | —                         | TODO   |
| 006  | SSRF guard on extract-pipeline fetches                         | P2       | M      | —                         | TODO   |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- No hard dependencies; all six are independently executable. Recommended order: 001 first (live critical CVE on the auth wall), 002 second (every later plan then gets a CI gate), then 003–006 in any order.
- 005 and 006 compose: the bookmarklet token is the URL-borne credential that makes SSRF (006) reachable by a non-owner; landing both closes the chain.
- 004 has an operator step **before deploy**: set `WORKER_SECRET` in Vercel/local env, and rotate the Supabase service-role key afterwards (it was previously sent as an HTTP header).

## Findings confirmed but not planned (backlog, in leverage order)

- **Pipeline test coverage** (`workers/jobs/extract.ts`, `workers/jobs/ai.ts`, `lib/extract/article|twitter|pdf|image.ts`, `lib/search/`, `lib/shell/feed-query.ts`, server actions, chat route — all untested; CLAUDE.md forbids skipping extraction tests). L effort. Must precede the two refactors below.
- **Detail-page query waterfall** — `lib/detail/load-item.ts:84-132` runs ~6 sequential Supabase round-trips (incl. a redundant two-step latest-insights fetch); parallelize with `Promise.all`. S effort.
- **Hybrid-search duplication** — `lib/search/index.ts` vs `retrieve()` in `app/api/chat/route.ts:180-231`; also FTS+semantic run sequentially in `searchLibrary`. M effort; wants characterization tests first.
- **Feed filter scalability** — `lib/shell/feed-query.ts:41-72` intersects unbounded ID arrays in JS (`ids.includes` O(n·m)); `LIST_LIMIT = 100` hard cap with no pagination. M effort; product input needed on pagination UX.
- **ESLint 8 EOL** → v9 upgrade (`.eslintrc.json` → flat config; `next lint` compatibility check). S–M effort.
- **`pdf-parse` abandoned upstream (last release 2018)** — accepted for now; fallback-to-metadata behavior makes it non-urgent. Document in `lib/extract/pdf.ts` when next touched.
- **Minor**: missing `.catch` on the undo callback (`components/shell/global-shortcuts.tsx:144`); `.env.example` lacks optional-var guidance + secret-generation one-liner; README Supabase setup step 3 assumes context.
- **Direction options** (maintainer's call, evidence in PRD): PWA + web-share-target (F-25, P1, zero code); weekly digest email (F-27, P1, zero code, needs an email-provider decision the locked stack doesn't cover); insight-feedback loop (thumbs data exists in `insight_cards.user_feedback`, nothing consumes it).

## Findings considered and rejected

- **".env committed with live keys"** (subagent report): FALSE — `git log --all -- .env` is empty; the file was never committed and `.gitignore` covers `.env*`. A local plaintext `.env` is standard practice. No rotation required on this evidence.
- **"LiveRefresh polls leak across itemId changes"**: FALSE — React runs effect cleanup on dependency change; `components/detail/live-refresh.tsx:37-41` clears both channel and interval correctly.
- **"Chat pays a 20s embedding timeout when VOYAGE_API_KEY is missing"**: FALSE — `lib/ai/embeddings.ts:52-53` returns null before any network call.
- **Prompt injection via extracted content**: by-design acceptable — `generateObject` with schema, no tool use, no exfiltration channel. Revisit only if tools are added to AI calls.
- **Refactor of 337-line `global-shortcuts.tsx`**: real debt, but rejected for now — MED breakage risk with zero test coverage on shortcuts; revisit after pipeline/UI test coverage lands.
- **RLS "defense-in-depth" on service-role queries**: current code filters by `user_id` everywhere it must; a doc-line in CLAUDE.md when next edited suffices.
- **Prompt versioning in DB**: premature before real feedback data accumulates; revisit post-launch.
